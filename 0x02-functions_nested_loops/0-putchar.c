#include <stdio.h>

/**
 * main - Prints combinations of two two-digit numbers
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch[10] = "_putchar";

	itn i;

	for (i = 0; i < 8; i++)
	{
		_putchar(ch[i]);
	}
	_putchar(10);
	return (0);
}
